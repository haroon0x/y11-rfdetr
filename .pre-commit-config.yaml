repos:
  - repo: local
    hooks:
      - id: export-requirements
        name: Export requirements.txt
        entry: python scripts/export_requirements.py
        language: python
        files: ^(pyproject\.toml)$
        pass_filenames: false
